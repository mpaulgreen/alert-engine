apiVersion: v1
kind: Secret
metadata:
  name: alert-engine-secrets
  namespace: alert-engine
  labels:
    app.kubernetes.io/name: alert-engine
    app.kubernetes.io/component: secrets
    app.kubernetes.io/part-of: log-monitoring-platform
type: Opaque
data:
  # Base64 encoded Slack webhook URL
  # To update: echo -n "https://hooks.slack.com/services/YOUR/WEBHOOK/URL" | base64
  SLACK_WEBHOOK_URL: ""  # Replace with actual base64 encoded webhook URL
stringData:
  # Alternative: Use stringData for plain text (will be automatically base64 encoded)
  # SLACK_WEBHOOK_URL: "https://hooks.slack.com/services/YOUR/WEBHOOK/URL" 